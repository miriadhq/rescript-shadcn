---
title: Combobox
description: Autocomplete input with a list of suggestions.
base: aria
component: true
links:
  doc: https://react-spectrum.adobe.com/react-aria/components.html
  api: https://react-spectrum.adobe.com/react-aria/components.html
---

<ComponentPreview
  name="ComboboxDemo"
  description="A combobox with a list of frameworks."
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @rescript-shadcn/Combobox
```

</TabsContent>
<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install react-aria-components rescript-react-aria
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource lib="aria"
  name="Combobox"
  title="components/ui/Combobox.res"
/>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```res showLineNumbers
let frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

<Combobox items=frameworks>
  <Combobox.Input placeholder="Select a framework" />
  <Combobox.Content>
    <Combobox.List
      renderEmptyState={_ =>
        <Combobox.Empty> {"No items found."->React.string} </Combobox.Empty>}
    >
      {item =>
        <Combobox.Item key=item value=item> {item->React.string} </Combobox.Item>}
    </Combobox.List>
  </Combobox.Content>
</Combobox>
```

## Custom Items

Set `textValue` on each item when its rendered children are not plain text.

```res showLineNumbers
type framework = {
  label: string
  value: string
}

let frameworks = [
  { label: "Next.js", value: "next" },
  { label: "SvelteKit", value: "sveltekit" },
  { label: "Nuxt", value: "nuxt" },
]

module ExampleComboboxCustomItems = {
  @react.component
  let make = () => {
    <Combobox items={frameworks}>
      <Combobox.Input placeholder="Select a framework" />
      <Combobox.Content>
        <Combobox.List
          renderEmptyState={_ =>
            <Combobox.Empty> {"No items found."->React.string} </Combobox.Empty>}
        >
          {framework =>
            <Combobox.Item
              key={framework.value}
              value={framework}
              textValue={framework.label}
            >
              {framework.label->React.string}
            </Combobox.Item>}
        </Combobox.List>
      </Combobox.Content>
    </Combobox>
  }
}
```

## Multiple Selection

Use `selectionMode=ReactAria.Combobox.Multiple` with chips for multi-select behavior.

```res showLineNumbers
let frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

module ExampleComboboxMultiple = {
  @react.component 
  let make = () =>
    <Combobox
      selectionMode=ReactAria.Combobox.Multiple
      items={frameworks}
      defaultValue={[frameworks->Array.getUnsafe(0)]}
      allowsEmptyCollection=true
    >
      <Combobox.Chips>
        <Combobox.ChipList>
          {item => <Combobox.Chip key=item id=item> {item->React.string} </Combobox.Chip>}
        </Combobox.ChipList>
        <Combobox.ChipsInput />
      </Combobox.Chips>
      <Combobox.Content>
        <Combobox.List
          renderEmptyState={_ =>
            <Combobox.Empty> {"No items found."->React.string} </Combobox.Empty>}
        >
          {item =>
            <Combobox.Item key=item value=item> {item->React.string} </Combobox.Item>}
        </Combobox.List>
      </Combobox.Content>
    </Combobox>
}
```

## Examples

### Basic

A simple combobox with a list of frameworks.

<ComponentPreview name="ComboboxBasic" />

### Multiple

A combobox with multiple selection using `selectionMode=Multiple` and chips.

<ComponentPreview name="ComboboxMultiple" />

### Clear Button

Use the `showClear` prop to show a clear button.

<ComponentPreview name="ComboboxClear" />

### Groups

Use `Combobox.Group`, `Combobox.Label`, and `Combobox.Separator` to group items.

<ComponentPreview name="ComboboxGroups" />

### Custom Items

You can render custom component inside `ComboboxItem`.

<ComponentPreview name="ComboboxCustom" />

### Invalid

Use `isInvalid` to make the combobox invalid.

<ComponentPreview name="ComboboxInvalid" />

### Disabled

Use `isDisabled` to disable the combobox.

<ComponentPreview name="ComboboxDisabled" />

### Auto Highlight

Use React Aria's `menuTrigger` and focus strategy props to control initial highlighting.

<ComponentPreview name="ComboboxAutoHighlight" />

### Popup

Use `Combobox.Trigger` with `Combobox.Value`, and move `Combobox.Input` inside
`Combobox.Content`, to present the combobox as a popup button.

<ComponentPreview name="ComboboxPopup" />

### Input Group

You can add an addon to the combobox by using the `InputGroupAddon` component inside the `ComboboxInput`.

<ComponentPreview name="ComboboxInputGroup" />


## API Reference

See the [React Aria](https://react-spectrum.adobe.com/react-aria/components.html) documentation for more information.
