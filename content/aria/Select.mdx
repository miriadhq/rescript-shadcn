---
title: Select
description: Displays a list of options for the user to pick from—triggered by a button.
base: aria
component: true
featured: true
links:
  doc: https://react-spectrum.adobe.com/react-aria/components.html
  api: https://react-spectrum.adobe.com/react-aria/components.html
---

<ComponentPreview name="SelectDemo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @rescript-shadcn/Select
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install react-aria-components rescript-react-aria
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource lib="aria"
  name="Select"
  title="components/ui/Select.res"
/>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```res showLineNumbers
<Select>
  <Select.Trigger className="w-[180px]">
    <Select.Value placeholder="Theme" />
  </Select.Trigger>
  <Select.Content>
    <Select.Group>
      <Select.Item value="light"> {"Light"->React.string} </Select.Item>
      <Select.Item value="dark"> {"Dark"->React.string} </Select.Item>
      <Select.Item value="system"> {"System"->React.string} </Select.Item>
    </Select.Group>
  </Select.Content>
</Select>
```

## Examples

### Align Item With Trigger

Use `placement`, `offset`, and `crossOffset` on `Select.Content` to control the
popover position. This example also demonstrates a default selection.

<ComponentPreview name="SelectAlignItem" />

### Groups

Use `Select.Group`, `Select.Label`, and `Select.Separator` to organize items.

<ComponentPreview name="SelectGroups" />

### Scrollable

A select with many items that scrolls.

<ComponentPreview name="SelectScrollable" />

### Disabled

<ComponentPreview name="SelectDisabled" />

### Invalid

Set `isInvalid` on `Select` and `dataInvalid` on its `Field` wrapper.

```res /data-invalid/ /aria-invalid/
<Field dataInvalid>
  <Field.Label> {"Fruit"->React.string} </Field.Label>
  <Select isInvalid>
    <Select.Trigger>
      <Select.Value />
    </Select.Trigger>
  </Select>
</Field>
```

<ComponentPreview name="SelectInvalid" />


## API Reference

See the [React Aria Select](https://react-spectrum.adobe.com/react-aria/components.html) documentation.
