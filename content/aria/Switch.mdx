---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
base: aria
component: true
links:
  doc: https://react-spectrum.adobe.com/react-aria/components.html
  api: https://react-spectrum.adobe.com/react-aria/components.html
---

<ComponentPreview name="SwitchDemo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @rescript-shadcn/Switch
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install react-aria-components rescript-react-aria
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource lib="aria"
  name="Switch"
  title="components/ui/Switch.res"
/>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```res
<Switch />
```

## Examples

### Description

<ComponentPreview name="SwitchDescription" />

### Choice Card

Card-style selection where `FieldLabel` wraps the entire `Field` for a clickable card pattern.

<ComponentPreview name="SwitchChoiceCard" />

### Disabled

Add `isDisabled` to `Switch` and `dataDisabled` to the `Field` wrapper.

<ComponentPreview name="SwitchDisabled" />

### Invalid

Add the `aria-invalid` prop to the `Switch` component to indicate an invalid state. Add the `data-invalid` prop to the `Field` component for styling.

<ComponentPreview name="SwitchInvalid" />

### Size

Use the `size` prop to change the size of the switch.

<ComponentPreview name="SwitchSizes" />


## API Reference

See the [React Aria Switch](https://react-spectrum.adobe.com/react-aria/components.html) documentation.
