// DirectoryList: placeholder for the shadcn registry directory browser.
// The original uses shadcn-specific hooks and registry data not applicable here.

@react.component
let make = () => {
  <div className="mt-6 text-muted-foreground text-sm">
    {"Directory list is not available in this context."->React.string}
  </div>
}
